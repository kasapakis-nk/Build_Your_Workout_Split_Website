// Tracking of the number of exercise rows being used/shown.
let currentNumOfExRows = -1;

// Checks if an Exercise is currently expanded (true) or not (false).
let isExExpanded = [];

// Biggest global variable item. 
// This will store all important (user input) data.
// Later this will be exportable etc. {


// Object containing all objects, 
// each of which pretains to a specific Ex.
let allExDetails = {allExercises: {}};


// }